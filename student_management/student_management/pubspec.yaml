name: student_management
description: "A Student Management System app for COMSATS University Islamabad, Vehari Campus. CSC303 - Mobile Application Development, Assignment No. 3"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Icons package (Material Icons already included in Flutter)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true